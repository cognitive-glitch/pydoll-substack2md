# Substack login credentials for premium content access
# Copy this file to .env and fill in your actual credentials

# Your Substack account email
SUBSTACK_EMAIL=your-email@example.com

# Your Substack account password
SUBSTACK_PASSWORD=your-password

# Optional: Default Substack URL to scrape
# DEFAULT_SUBSTACK_URL=https://example.substack.com/

# Optional: Number of posts to scrape (0 = all posts)
# NUM_POSTS_TO_SCRAPE=0

# Optional: Enable premium scraping by default
# USE_PREMIUM=false

# Optional: Run browser in headless mode (default: false to allow user intervention)
# HEADLESS=false

# Optional: Custom Chrome/Edge binary path
# BROWSER_PATH=/path/to/chrome

# Optional: Custom user agent
# USER_AGENT=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36
